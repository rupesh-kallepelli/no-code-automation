{{- range $i, $e := until (int .Values.replicaCount) }}
apiVersion: v1
kind: Service
metadata:
  name: kafka-{{ $i }}-ext
  labels:
    app: kafka
spec:
  selector:
    statefulset.kubernetes.io/pod-name: kafka-{{ $i }}
  ports:
    - port: 9092
      targetPort: 9092
---
{{- end }}
