apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "ai-test-planner.fullname" . }}
  labels:
    {{- include "ai-test-planner.labels" . | nindent 4 }}
spec:
  to:
    kind: Service
    name: {{ include "ai-test-planner.fullname" . }}
  port:
    targetPort: http
  tls:
    termination: edge