apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-test-planner.fullname" . }}
  labels:
    {{- include "ai-test-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-test-planner.selectorLabels" . | nindent 4 }}
