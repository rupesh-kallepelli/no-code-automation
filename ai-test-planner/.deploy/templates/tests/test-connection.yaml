apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-test-planner.fullname" . }}-test-connection"
  labels:
    {{- include "ai-test-planner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-test-planner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
