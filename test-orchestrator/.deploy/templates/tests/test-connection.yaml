apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-orchestrator.fullname" . }}-test-connection"
  labels:
    {{- include "test-orchestrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-orchestrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
