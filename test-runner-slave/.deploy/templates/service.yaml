apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-runner-slave.fullname" . }}
  labels:
    {{- include "test-runner-slave.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "test-runner-slave.selectorLabels" . | nindent 4 }}
