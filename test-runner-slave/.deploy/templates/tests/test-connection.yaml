apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-runner-slave.fullname" . }}-test-connection"
  labels:
    {{- include "test-runner-slave.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-runner-slave.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
