server:
  port: 8080
spring:
  application:
    name: test-runner-slave
  data:
    redis:
      host: localhost
      port: 6379
      password: StrongPassword123!
browser:
  service: http://localhost:8081/api/v1
logging:
  level:
    com:
      vr:
        test:
          runner:
            slave: debug
#browser:
#  service: https://browser-service-registry-kallepallirupesh-dev.apps.rm2.thpm.p1.openshiftapps.com/api/v1